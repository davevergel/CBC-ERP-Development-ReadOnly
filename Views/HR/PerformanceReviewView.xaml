<UserControl x:Class="CbcRoastersErp.Views.HR.PerformanceReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Performance Reviews" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Reviews}" SelectedItem="{Binding SelectedReview}" AutoGenerateColumns="False" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding ReviewDate}" Width="100"/>
                <DataGridTextColumn Header="Reviewer" Binding="{Binding Reviewer}" Width="150"/>
                <DataGridTextColumn Header="Score" Binding="{Binding Score}" Width="65"/>
                <DataGridTextColumn Header="Comments" Binding="{Binding Comments}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Header="Review Details" Grid.Row="2" Padding="10">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Reviewer:" Width="100"/>
                    <TextBox Text="{Binding SelectedReview.Reviewer}" Width="200"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Score:" Width="100"/>
                    <TextBox Text="{Binding SelectedReview.Score}" Width="200"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Comments:" Width="100"/>
                    <TextBox Text="{Binding SelectedReview.Comments}" Width="300"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,10" HorizontalAlignment="Center">
                    <Button Content="Load" Command="{Binding LoadCommand}" Width="75" Margin="5"/>
                    <Button Content="Add" Command="{Binding AddCommand}" Width="75" Margin="5"/>
                    <Button Content="Update" Command="{Binding UpdateCommand}" Width="75" Margin="5"/>
                    <Button Content="Delete" Command="{Binding DeleteCommand}" Width="75" Margin="5"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>