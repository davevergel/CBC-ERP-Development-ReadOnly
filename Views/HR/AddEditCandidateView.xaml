<UserControl x:Class="CbcRoastersErp.Views.HR.AddEditCandidateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:CbcRoastersErp.Views.HR"
             xmlns:vm="clr-namespace:CbcRoastersErp.ViewModels.HR"
             Background="{DynamicResource MaterialDesignPaper}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="550">
    <Grid>
        <StackPanel>
            <Border Background="#2c3e50" Padding="20" >
                <TextBlock Text="Add/Edit Candidate" Foreground="White"
                           FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" />
            </Border>
            <TextBox materialDesign:HintAssist.Hint="First Name"
                     Text="{Binding Candidate.FirstName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <TextBox materialDesign:HintAssist.Hint="Last Name"
                        Text="{Binding Candidate.LastName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            <TextBox materialDesign:HintAssist.Hint="Email"
                        Text="{Binding Candidate.Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            <TextBox materialDesign:HintAssist.Hint="Phone"
                        Text="{Binding Candidate.Phone, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <ComboBox materialDesign:HintAssist.Hint="Position Applied For"
                        SelectedItem="{Binding Candidate.PositionAppliedFor, UpdateSourceTrigger=PropertyChanged}" Margin="0,5">
                <ComboBoxItem Content="Barista"/>
                <ComboBoxItem Content="Roaster"/>
                <ComboBoxItem Content="Sales"/>
                <ComboBoxItem Content="Manager"/>
                <ComboBoxItem Content="HR"/>
                <ComboBoxItem Content="Marketing"/>
                <ComboBoxItem Content="Finance"/>
                <ComboBoxItem Content="IT"/>
                <ComboBoxItem Content="Customer Service"/>
                <ComboBoxItem Content="Warehouse"/>
                <ComboBoxItem Content="Quality Control"/>
                <ComboBoxItem Content="Logistics"/>
                <ComboBoxItem Content="Research and Development"/>
                <ComboBoxItem Content="Supply Chain"/>
                <ComboBoxItem Content="Maintenance"/>
                <ComboBoxItem Content="Other"/>
            </ComboBox>
            
            <TextBox materialDesign:HintAssist.Hint="Resume Link"
                        Text="{Binding Candidate.ResumeLink, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
           
            <TextBox materialDesign:HintAssist.Hint="Application Date"
                        Text="{Binding Candidate.ApplicationDate, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <ComboBox materialDesign:HintAssist.Hint="Status"
                        SelectedItem="{Binding Candidate.Status, UpdateSourceTrigger=PropertyChanged}" Margin="0,5">
                <ComboBoxItem Content="New"/>
                <ComboBoxItem Content="In Review"/>
                <ComboBoxItem Content="Interview Scheduled"/>
                <ComboBoxItem Content="Hired"/>
                <ComboBoxItem Content="Rejected"/>
            </ComboBox>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Save" Command="{Binding SaveCommand}" Margin="5"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5"/>
            </StackPanel>


        </StackPanel>
            
    </Grid>
</UserControl>
