<UserControl x:Class="CbcRoastersErp.Views.HR.JobPostingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Job Postings" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding JobPostings}" SelectedItem="{Binding SelectedPosting}" AutoGenerateColumns="False" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="150"/>
                <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="Posted" Binding="{Binding PostedDate}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Header="Posting Details" Grid.Row="2" Padding="10">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Title:" Width="100"/>
                    <TextBox Text="{Binding SelectedPosting.Title}" Width="200"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Department:" Width="100"/>
                    <TextBox Text="{Binding SelectedPosting.Department}" Width="200"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Status:" Width="100"/>
                    <ComboBox Width="200" SelectedItem="{Binding SelectedPosting.Status}">
                        <ComboBoxItem Content="Open"/>
                        <ComboBoxItem Content="Closed"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Description:" Width="100"/>
                    <TextBox Text="{Binding SelectedPosting.Description}" Width="300"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,10" HorizontalAlignment="Center">
                    <Button Content="Load" Command="{Binding LoadCommand}" Width="75" Margin="5"/>
                    <Button Content="Add" Command="{Binding AddCommand}" Width="75" Margin="5"/>
                    <Button Content="Update" Command="{Binding UpdateCommand}" Width="80" Margin="5"/>
                    <Button Content="Delete" Command="{Binding DeleteCommand}" Width="75" Margin="5"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>