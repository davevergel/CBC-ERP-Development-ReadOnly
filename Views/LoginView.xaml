<UserControl x:Class="CbcRoastersErp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock Text="CBC Roasters ERP Managment" HorizontalAlignment="Center" FontSize="24"/>
        </StackPanel>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Username"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="200"/>

            <TextBlock Text="Password"/>
            <PasswordBox x:Name="PasswordBox" Width="200" PasswordChanged="PasswordBox_PasswordChanged"/>

            <Button Content="Login" Command="{Binding LoginCommand}" CommandParameter="Execute" Width="200"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red"/>
        </StackPanel>
    </Grid>
</UserControl>
