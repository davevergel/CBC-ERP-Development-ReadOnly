<UserControl x:Class="CbcRoastersErp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource BackgroundBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Company Logo -->
        <StackPanel Grid.Row="0" Margin="0 0 0 20" HorizontalAlignment="Center">
            <Image Source="/Views/CBC_Logo_500x500.png"
                   Width="100" Height="100"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality"/>
        </StackPanel>

        <!-- Login Form -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="10">
            <TextBlock Text="CBC Roasters ERP Login"
                       Style="{StaticResource Heading1TextBlock}"
                       HorizontalAlignment="Center"/>

            <TextBox materialDesign:HintAssist.Hint="Username"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Width="250"/>

            <PasswordBox x:Name="PasswordBox"
                         materialDesign:HintAssist.Hint="Password"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         Width="250"
                         PasswordChanged="PasswordBox_PasswordChanged"/>

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="Red"
                       HorizontalAlignment="Center"
                       FontSize="12"/>

            <Button Content="Login"
                    Command="{Binding LoginCommand}" CommandParameter="Execute"
                    Style="{StaticResource PrimaryButton}"
                    Width="250" Height="40"/>
        </StackPanel>

        <!-- Footer -->
        <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0 20 0 0">
            <TextBlock Text="© CBC Roasters.com"
                       Style="{StaticResource CaptionTextBlock}"/>
        </StackPanel>
    </Grid>
</UserControl>
