<UserControl x:Class="CbcRoastersErp.Views.Administration.MasterData.MasterDataDashDbView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Administration.MasterData"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource MaterialDesignPaper}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer>
            <StackPanel Margin="20" VerticalAlignment="Top">
                <!-- Header -->
                <TextBlock Text="Master Data"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryHueDarkBrush}"
                       Margin="0 0 0 10"/>

                <!-- Back to Dashboard -->
                <Button Content="Back to Dashboard"
                Command="{Binding NavigateBackCommand}"
                HorizontalAlignment="Left"
                Width="150"
                Margin="0,0,0,20"/>
                <!-- Finished Goods-->
                <GroupBox Header="Operations Master Data">
                    <WrapPanel Margin="10" ItemHeight="130" ItemWidth="250">
                            <materialDesign:Card Margin="10">
                            <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding NavigateToManageFinishedGoodsCommand}" Margin="0,39,0,38">
                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Cube" Margin="0 0 8 0"/>
                                        <TextBlock Text="Finished Goods" FontSize="18" FontWeight="Bold"/>                                        
                                    </StackPanel>
                                    <TextBlock Text="Manage inventory items" Foreground="Gray"/>
                                </StackPanel>
                            </Button>
                        </materialDesign:Card>
                        <materialDesign:Card Margin="10">
                            <Button Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding NavigateToManageRoastingProfilesCommand}">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="CoffeeMaker" Margin="0 0 8 0"/>
                                        <TextBlock Text="Roasting Profiles" FontSize="18" FontWeight="Bold"/>
                                    </StackPanel>
                                    <TextBlock Text="Manage roasting profiles" Foreground="Gray"/>
                                </StackPanel>
                            </Button>
                        </materialDesign:Card>
                    </WrapPanel>
                </GroupBox>
            </StackPanel>

        </ScrollViewer>
        

    </Grid>
</UserControl>
