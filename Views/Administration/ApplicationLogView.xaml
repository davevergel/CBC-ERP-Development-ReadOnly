<UserControl x:Class="CbcRoastersErp.Views.ApplicationLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:CbcRoastersErp.Views"
             xmlns:vm="clr-namespace:CbcRoastersErp.ViewModels" d:DataContext="{d:DesignInstance Type=vm:ApplicationLogViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200">
    <Grid>
        <DockPanel Background="#f8f9fa" LastChildFill="True">
            <Border Background="#2c3e50" Padding="20" DockPanel.Dock="Top">
                <TextBlock Text="Application Logs" Foreground="White" FontSize="22" FontWeight="SemiBold" />
            </Border>

            <StackPanel Margin="10" DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <TextBox Width="200" Margin="5"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         materialDesign:HintAssist.Hint="Search Logs..." />
                <Button Content="Search" Command="{Binding SearchCommand}" VerticalAlignment="Center" Margin="0,0,10,0" />
                <Button Content="Back to Dashboard" Command="{Binding NavigateBackCommand}" 
                        Margin="0,0,10,0" Width="200"
                        Style="{StaticResource MaterialDesignRaisedButton}" />
            </StackPanel>

            <!-- Pagination Controls -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" 
                Margin="10" DockPanel.Dock="Bottom">
                <Button Content="Previous" Command="{Binding PageChangedCommand}" 
                        CommandParameter="Previous"
                Margin="0,0,5,0" />
                <TextBlock Text="{Binding CurrentPage}" Margin="0,0,5,0" />
                <TextBlock Text="/" Margin="0,0,5,0" />
                <TextBlock Text="{Binding TotalPages}" Margin="0,0,5,0" />
                <Button Content="Next" Command="{Binding PageChangedCommand}"
                        CommandParameter="Next"/>
            </StackPanel>

            <!-- Log Table -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <DataGrid ItemsSource="{Binding Logs}" AutoGenerateColumns="False" Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" Width="150" />
                        <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="100" />
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="300" />
                        <DataGridTextColumn Header="Method" Binding="{Binding Method}" Width="200" />
                        <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="200" />
                        <DataGridTextColumn Header="User" Binding="{Binding UserName}" Width="100" />
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>
        </DockPanel>
    </Grid>

</UserControl>
