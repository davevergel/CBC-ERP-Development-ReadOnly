<UserControl x:Class="CbcRoastersErp.Views.Finance.FinanceDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Finance"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             Height="Auto" Width="Auto">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Finance Dashboard" FontSize="24" FontWeight="Bold" Margin="0 0 0 20" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0 0 20 0">
                <TextBlock Text="Revenue" FontWeight="Bold"/>
                <TextBlock Text="{Binding Revenue, StringFormat=C}" FontSize="18" Foreground="Green" />

                <TextBlock Text="COGS" FontWeight="Bold" Margin="0 20 0 0"/>
                <TextBlock Text="{Binding COGS, StringFormat=C}" FontSize="18" Foreground="Red" />

                <TextBlock Text="Operating Expenses" FontWeight="Bold" Margin="0 20 0 0"/>
                <TextBlock Text="{Binding OperatingExpenses, StringFormat=C}" FontSize="18" Foreground="DarkRed" />
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="Net Income" FontWeight="Bold"/>
                <TextBlock Text="{Binding NetIncome, StringFormat=C}" FontSize="18" Foreground="Blue" />

                <TextBlock Text="Open PO Liabilities" FontWeight="Bold" Margin="0 20 0 0"/>
                <TextBlock Text="{Binding OpenPoLiabilities, StringFormat=C}" FontSize="18" Foreground="Orange" />
            </StackPanel>          
        </Grid>

        <Grid Grid.Row="2">
            <StackPanel>
                <TextBlock Text="Net Income Trends" FontWeight="Bold" FontSize="16" Margin="0 20 0 10"/>
                <lvc:CartesianChart Series="{Binding IncomeSeries}" Height="250">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Month" Labels="{Binding MonthLabels}" />
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Net Income" LabelFormatter="{Binding YFormatter}" />
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </StackPanel>
        </Grid>

        <Button Grid.Row="2" Content="Back" Command="{Binding NavigateBackCommand}" 
            HorizontalAlignment="Left" Margin="0 20 0 0" />
    </Grid>

</UserControl>
