<UserControl x:Class="CbcRoastersErp.Views.Finance.FinanceDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Finance"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Height="Auto" Width="Auto">
    <Grid Margin="20">        
        <StackPanel>
            <Button Content="Back" Command="{Binding NavigateBackCommand}" Style="{StaticResource MaterialDesignFlatDarkBgButton }" Margin="5"/>
            <TextBlock Text="Finance Dashboard" FontSize="20" FontWeight="Bold" 				Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Revenue:" FontWeight="SemiBold" Width="120"/>
                <TextBlock Text="{Binding Revenue}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="COGS:" FontWeight="SemiBold" Width="120"/>
                <TextBlock Text="{Binding COGS}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Operating Expenses:" FontWeight="SemiBold" Width="120"/>
                <TextBlock Text="{Binding OperatingExpenses}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Net Income:" FontWeight="SemiBold" Width="120"/>
                <TextBlock Text="{Binding NetIncome}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
