<UserControl x:Class="CbcRoastersErp.Views.Finance.DriposDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:CbcRoastersErp.ViewModels.Finance"
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Finance"
             mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filters -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 0 0 10">
            <DatePicker SelectedDate="{Binding StartDate}" Margin="0 0 10 0"/>
            <DatePicker SelectedDate="{Binding EndDate}" Margin="0 0 10 0"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="0 0 10 0"/>
            <Button Content="Back" Command="{Binding NavigateBackCommand}" Margin="0 0 10 0"/>
            <ToggleButton Content="Pie Chart" IsChecked="{Binding ShowPieChart}" Margin="0 0 10 0"/>
        </StackPanel>

        <!-- Charts -->
        <Grid Grid.Row="2">
            <lvc:CartesianChart x:Name="BarChart"
                                Series="{Binding SeriesCollection}"
                                LegendLocation="Right"
                                Visibility="{Binding ShowPieChart, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Labels="{Binding Labels}" LabelsRotation="15" />
                </lvc:CartesianChart.AxisX>
            </lvc:CartesianChart>

            <lvc:PieChart x:Name="PieChart"
                          Series="{Binding PieSeriesCollection}"
                          LegendLocation="Right"
                          Visibility="{Binding ShowPieChart, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</UserControl>