<UserControl x:Class="CbcRoastersErp.Views.Finance.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Finance"
             xmlns:vm="clr-namespace:CbcRoastersErp.ViewModels.Finance"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.DataContext>
        <vm:AccountViewModel/>
    </UserControl.DataContext>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Load" Command="{Binding LoadCommand}" Style="{StaticResource MaterialDesignFlatDarkBgButton}" Margin="5"/>
            <Button Content="Add/Edit" Command="{Binding OpenAddEditAccountCommand}" Style="{StaticResource MaterialDesignFlatDarkBgButton}" Margin="5"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}" Style="{StaticResource MaterialDesignFlatDarkBgButton }" Background="Red" Margin="5"/>
            <Button Content="Back" Command="{Binding NavigateBackCommand}" Style="{StaticResource MaterialDesignFlatDarkBgButton }"  Margin="5"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Accounts}"
                  SelectedItem="{Binding SelectedAccount}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Account Name" Binding="{Binding AccountName}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding AccountType}" Width="150"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
