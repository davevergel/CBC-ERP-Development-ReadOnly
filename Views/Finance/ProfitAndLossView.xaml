<UserControl x:Class="CbcRoastersErp.Views.Finance.ProfitAndLossView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Finance"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Profit and Loss Statement" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="From:" Margin="0,0,5,0"/>
                <DatePicker SelectedDate="{Binding StartDate}" Margin="0,0,20,0"/>
                <TextBlock Text="To:" Margin="0,0,5,0"/>
                <DatePicker SelectedDate="{Binding EndDate}" Margin="0,0,10,0"/>
                <Button Content="Generate" Command="{Binding GenerateCommand}" Style="{StaticResource MaterialDesignFlatDarkBgButton}" />
                <Button Content="Back" Command="{Binding NavigateBackCommand}" Style="{StaticResource MaterialDesignFlatDarkBgButton}" Margin="5" />
            </StackPanel>

            <StackPanel Margin="0,10">
                <TextBlock Text="Revenue" FontWeight="Bold"/>
                <TextBlock Text="{Binding TotalRevenue, StringFormat=C}" Margin="0,0,0,10"/>

                <TextBlock Text="Cost of Goods Sold" FontWeight="Bold"/>
                <TextBlock Text="{Binding TotalCOGS, StringFormat=C}" Margin="0,0,0,10"/>

                <TextBlock Text="Operating Expenses" FontWeight="Bold"/>
                <TextBlock Text="{Binding TotalOperatingExpenses, StringFormat=C}" Margin="0,0,0,10"/>

                <TextBlock Text="Net Income" FontWeight="Bold"/>
                <TextBlock Text="{Binding NetIncome, StringFormat=C}" Foreground="Green"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
