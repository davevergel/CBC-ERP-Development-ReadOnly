<UserControl x:Class="CbcRoastersErp.Views.Finance.AddEditAccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Finance"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Add/Edit Account" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

            <TextBox materialDesign:HintAssist.Hint="Account Name"
                     Text="{Binding Item.AccountName}" Margin="0,5"/>

            <ComboBox materialDesign:HintAssist.Hint="Account Type"
                      SelectedItem="{Binding Item.AccountType}" Margin="0,5">
                <ComboBoxItem Content="Asset"/>
                <ComboBoxItem Content="Liability"/>
                <ComboBoxItem Content="Equity"/>
                <ComboBoxItem Content="Revenue"/>
                <ComboBoxItem Content="Expense"/>
            </ComboBox>

            <ComboBox materialDesign:HintAssist.Hint="Category"
                      SelectedItem="{Binding Item.Category}" Margin="0,5">
                <ComboBoxItem Content="COGS"/>
                <ComboBoxItem Content="OperatingExpense"/>
                <ComboBoxItem Content="NonOperatingExpense"/>
                <ComboBoxItem Content="Revenue"/>
                <ComboBoxItem Content="Asset"/>
                <ComboBoxItem Content="Liability"/>
                <ComboBoxItem Content="Other"/>
            </ComboBox>

            <CheckBox Content="Active" IsChecked="{Binding Item.IsActive}" Margin="0,5"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
                <Button Content="Save" Command="{Binding SaveCommand}" Margin="5"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
