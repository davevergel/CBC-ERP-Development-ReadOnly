<UserControl x:Class="CbcRoastersErp.Views.Finance.AddEditJournalEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Finance"             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Add/Edit Journal Entry" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

            <DatePicker materialDesign:HintAssist.Hint="Entry Date"
                        SelectedDate="{Binding Entry.EntryDate, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>
            <TextBox materialDesign:HintAssist.Hint="Description"
                     Text="{Binding Entry.Description}" Margin="0,5"/>

            <TextBlock Text="Line Items" FontWeight="Bold" Margin="0,10,0,5"/>

            <DataGrid ItemsSource="{Binding Entry.Lines}" AutoGenerateColumns="False"
                      CanUserAddRows="True" CanUserDeleteRows="True" Margin="0,0,0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Account ID" Binding="{Binding AccountID, Mode=TwoWay}" Width="*"/>
                    <DataGridTextColumn Header="Account Name" Binding="{Binding AccountName, Mode=TwoWay}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, Mode=TwoWay}" Width="*"/>
                    <DataGridCheckBoxColumn Header="Is Debit" Binding="{Binding IsDebit, Mode=TwoWay}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
                <Button Content="Save Entry" Command="{Binding SaveCommand}" Margin="5"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
