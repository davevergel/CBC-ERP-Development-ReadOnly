<UserControl x:Class="CbcRoastersErp.Views.Purchasing.PurchaseOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Purchasing"
             xmlns:vm="clr-namespace:CbcRoastersErp.ViewModels.Purchasing"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Purchase Orders" FontSize="24" FontWeight="Bold" Margin="0 0 0 10"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding PurchaseOrders}"
                  SelectedItem="{Binding SelectedOrder, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  Margin="0 10 0 10"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding PurchaseOrderId }" Width="Auto"/>
                <DataGridTextColumn Header="PO Number" Binding="{Binding po_number}" Width="Auto" />
                <DataGridTextColumn Header="Supplier" Binding="{Binding SupplierName}" Width="*"/>
                <DataGridTextColumn Header="Order Date" Binding="{Binding OrderDate}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount}" Width="*"/>
                <DataGridTextColumn Header="Invoice PDF" Binding="{Binding InvoicePdfPath}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10 0 0">
            <Button Content="Back" Command="{Binding NavigateBackCommand}" Width="80" Margin="10"/>
            <Button Content="Add/Edit" Command="{Binding OpenAddEditPurchaseCommand}" Width="100" Margin="10"/>
            <Button Content="Delete" Command="{Binding DeleteCommand }" Width="80" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>
