<UserControl x:Class="CbcRoastersErp.Views.Operations.Planning.AddEditFarmersMarketScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:Converter="clr-namespace:CbcRoastersErp.Helpers.converters"
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Operations.Planning"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <DockPanel Margin="0,0,0,10">
            <Image Source="/Views/CBC_Logo_500x500.png"
                   Width="50" Height="50"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   DockPanel.Dock="Left"/>
            <TextBlock Text="{Binding Schedule.Id,  Converter={StaticResource NullOrZeroToAddEditTitleConverter}, ConverterParameter='Farmers Market Production Schedule'}"
                       Style="{StaticResource Heading1TextBlock}"
                       FontSize="24"
                       FontWeight="Bold"
                       Grid.Row="0" Margin="0,0,0,10"/>
        </DockPanel>

        <!-- Form Fields -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Market Date:" VerticalAlignment="Center" Width="100"/>
                <DatePicker SelectedDate="{Binding Schedule.MarketDate}" Width="200"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Notes:" VerticalAlignment="Top" Width="100"/>
                <TextBox Text="{Binding Schedule.Notes}" Width="300" AcceptsReturn="True" Height="100"/>
            </StackPanel>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <Button Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"/>
        </StackPanel>
    </Grid>
</UserControl>
