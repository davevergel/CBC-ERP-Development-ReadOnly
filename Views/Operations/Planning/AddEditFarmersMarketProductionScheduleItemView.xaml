<UserControl x:Class="CbcRoastersErp.Views.Operations.Planning.AddEditFarmersMarketProductionScheduleItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Operations.Planning"
             xmlns:Converter="clr-namespace:CbcRoastersErp.Helpers.converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel  Orientation="Horizontal" Margin="0 0 0 20">
            <Image Source="/Views/CBC_Logo_500x500.png"
                   Width="50"
                   Height="50"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality"/>
            <TextBlock Text="{Binding Path=Item.Id, Converter={StaticResource NullOrZeroToAddEditTitleConverter}, ConverterParameter='Schedule Item'}"  
                       Style="{StaticResource Heading1TextBlock}"
                       HorizontalAlignment="Center"
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
        </StackPanel>

        <!-- Fields -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Usage Type:" Width="120" VerticalAlignment="Center"/>
                <ComboBox SelectedItem="{Binding Item.UsageType}" Width="200">
                    <ComboBoxItem Content="BaggedRetail"/>
                    <ComboBoxItem Content="Brewing"/>
                    <ComboBoxItem Content="ColdBrew"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Product:" Width="120" VerticalAlignment="Center"/>
                <ComboBox ItemsSource="{Binding ProductList}"
              SelectedValue="{Binding Item.ProductId}"
              SelectedValuePath="FinishedGoodID"
              DisplayMemberPath="ProductName"
              Width="300"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Planned Qty:" Width="120" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Item.PlannedQuantity}" Width="200"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Actual Produced:" Width="120" VerticalAlignment="Center"/>
                <TextBox Text="{Binding Item.ActualProducedQuantity}" Width="200"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Roast Date:" Width="120" VerticalAlignment="Center"/>
                <DatePicker SelectedDate="{Binding Item.RoastDate}" Width="200"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Notes:" Width="120" VerticalAlignment="Top"/>
                <TextBox Text="{Binding Item.Notes}" Width="300" AcceptsReturn="True" Height="80"/>
            </StackPanel>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2">
            <Button Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
