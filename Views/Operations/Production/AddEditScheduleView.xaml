<UserControl x:Class="CbcRoastersErp.Views.AddEditScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Employee" />
            <ComboBox ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}" DisplayMemberPath="FullName" Width="300"/>
            <TextBlock Text="Task Description"/>
            <TextBox Text="{Binding Schedule.TaskDescription}" Width="300"/>

            <TextBlock Text="Total Hours"/>
            <TextBox Text="{Binding Schedule.TotalHours}" Width="300"/>

            <TextBlock Text="Break Duration (minutes)"/>
            <TextBox Text="{Binding Schedule.BreakDuration}" Width="300"/>

            <TextBlock Text="Status"/>
            <ComboBox SelectedValue="{Binding Schedule.Status}" Width="300">
                <ComboBoxItem Content="Scheduled"/>
                <ComboBoxItem Content="Completed"/>
                <ComboBoxItem Content="Missed"/>
            </ComboBox>

            <TextBlock Text="Start Date"/>
            <DatePicker SelectedDate="{Binding Schedule.StartDate}" Width="300"/>

            <TextBlock Text="Start Time"/>
            <TextBox Text="{Binding Schedule.StartTime}" Width="300"/>

            <TextBlock Text="End Date"/>
            <DatePicker SelectedDate="{Binding Schedule.EndDate}" Width="300"/>

            <TextBlock Text="End Time"/>
            <TextBox Text="{Binding Schedule.EndTime}" Width="300"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="20">
                <Button Content="Save" Command="{Binding SaveCommand}" Width="100" Margin="10"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Margin="10"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
