<UserControl x:Class="CbcRoastersErp.Views.Operations.SupplierEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Operations"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Background="{DynamicResource BackgroundBrush}"
             d:DesignWidth="800">

    <Grid Margin="30">
        <materialDesign:Card Padding="30">
            <StackPanel Margin="15">
                <StackPanel Orientation="Horizontal" Margin="10">
                    <!-- Logo -->
                    <Image Source="/Views/CBC_Logo_500x500.png"
                           Width="50" Height="50"
                           Stretch="Uniform"
                           RenderOptions.BitmapScalingMode="HighQuality" />
                    <!-- Header -->
                    <TextBlock Text="Supplier Details"
                               Style="{StaticResource Heading1TextBlock}"
                               Foreground="{DynamicResource PrimaryHueDarkBrush}"
                               Margin="30"/>
                </StackPanel>

                <!-- Name -->
                <TextBox Text="{Binding Supplier.Supplier_Name}"
                         materialDesign:HintAssist.Hint="Name"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}" />

                <!-- Email -->
                <TextBox Text="{Binding Supplier.Contact_email}"
                         materialDesign:HintAssist.Hint="Email"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}" />

                <!-- Phone -->
                <TextBox Text="{Binding Supplier.Contact_phone}"
                         materialDesign:HintAssist.Hint="Phone"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}" />

                <!-- Address -->
                <TextBox Text="{Binding Supplier.Address}"
                         materialDesign:HintAssist.Hint="Address"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}" />

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10 0 0">
                    <Button Content="Cancel"
                            Command="{Binding CancelCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Width="100" />
                    <Button Content="Save"
                            Command="{Binding SaveCommand}"
                            Style="{StaticResource PrimaryButton}"
                            Width="100" />
                </StackPanel>

            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
