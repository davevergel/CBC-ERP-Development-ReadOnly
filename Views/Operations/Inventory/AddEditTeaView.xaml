<UserControl x:Class="CbcRoastersErp.Views.AddEditTeaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="Tea Inventory Details" FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,10,0,0"/>

        <StackPanel Margin="20">
            <TextBlock Text="Tea Name"/>
            <TextBox Text="{Binding Tea.TeaName}" Width="300"/>

            <TextBlock Text="Type" />
            <TextBox Text="{Binding Tea.TeaType}" Width="300"/>
            
            <TextBlock Text="Origin"/>
            <TextBox Text="{Binding Tea.Origin}" Width="300"/>

            <TextBlock Text="Stock Level"/>
            <TextBox Text="{Binding Tea.StockLevel}" Width="300"/>

            <TextBlock Text="Price"/>
            <TextBox Text="{Binding Tea.Price}" Width="300"/>

            <TextBlock Text="Supplier"/>
            <ComboBox ItemsSource="{Binding SuppliersList}" 
              SelectedItem="{Binding SelectedSupplier}" 
              DisplayMemberPath="Supplier_Name"
              Width="300"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button Content="Save" Command="{Binding SaveCommand}" Width="100" Margin="5"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
