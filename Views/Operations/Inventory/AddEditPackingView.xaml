<UserControl x:Class="CbcRoastersErp.Views.AddEditPackingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="Packing Material Details" FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,10,0,0"/>

        <StackPanel Margin="20">
            <TextBlock Text="Material Name"/>
            <TextBox Text="{Binding PackingMaterial.MaterialName}" Width="300"/>

            <TextBlock Text="Stock Level"/>
            <TextBox Text="{Binding PackingMaterial.StockLevel}" Width="300"/>

            <TextBlock Text="Unit"/>
            <TextBox Text="{Binding PackingMaterial.Unit}" Width="300"/>

            <TextBlock Text="Last Updated"/>
            <DatePicker SelectedDate="{Binding PackingMaterial.LastUpdated}" Width="300"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button Content="Save" Command="{Binding SaveCommand}" Width="100" Margin="5"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
