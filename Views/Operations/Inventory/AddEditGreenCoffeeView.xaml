<UserControl x:Class="CbcRoastersErp.Views.AddEditGreenCoffeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Border Background="White" Padding="20" CornerRadius="10">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <TextBlock Text="Green Coffee Details" FontSize="20" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Form Fields -->
                <Grid Grid.Row="1" Margin="0,10,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Coffee Name:" Grid.Row="0" Grid.Column="0" Margin="0,5"/>
                    <TextBox Text="{Binding GreenCoffee.CoffeeName}" Grid.Row="0" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Origin:" Grid.Row="1" Grid.Column="0" Margin="0,5"/>
                    <TextBox Text="{Binding GreenCoffee.Origin}" Grid.Row="1" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Processing Method:" Grid.Row="2" Grid.Column="0" Margin="0,5"/>
                    <TextBox Text="{Binding GreenCoffee.ProcessType}" Grid.Row="2" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Variety:" Grid.Row="3" Grid.Column="0" Margin="0,5"/>
                    <TextBox Text="{Binding GreenCoffee.Variety}" Grid.Row="3" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Certifications:" Grid.Row="4" Grid.Column="0" Margin="0,5"/>
                    <TextBox Text="{Binding GreenCoffee.Certifications}" Grid.Row="4" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Harvest Year:" Grid.Row="5" Grid.Column="0" Margin="0,5"/>
                    <TextBox Text="{Binding GreenCoffee.HarvestYear}" Grid.Row="5" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Stock Level:" Grid.Row="6" Grid.Column="0" Margin="0,5"/>
                    <TextBox Text="{Binding GreenCoffee.StockLevel}" Grid.Row="6" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Price:" Grid.Row="7" Grid.Column="0" Margin="0,5"/>
                    <TextBox Text="{Binding GreenCoffee.Price}" Grid.Row="7" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Supplier:" Grid.Row="8" Grid.Column="0" Margin="0,5"/>
                    <ComboBox ItemsSource="{Binding SuppliersList}" 
                              SelectedItem="{Binding SelectedSupplier}" 
                              DisplayMemberPath="Supplier_Name"
                              Grid.Row="8" Grid.Column="1" Margin="5"/>

                    <TextBlock Text="Date Received:" Grid.Row="9" Grid.Column="0" Margin="0,5"/>
                    <DatePicker SelectedDate="{Binding GreenCoffee.DateReceived}" 
                                Grid.Row="9" Grid.Column="1" Margin="5"/>
                </Grid>

                <!-- Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Save" Command="{Binding SaveCommand}" Width="100" Margin="10,0"/>
                    <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Margin="10,0"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Border>
</UserControl>
