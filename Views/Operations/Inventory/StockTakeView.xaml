<UserControl x:Class="CbcRoastersErp.Views.Operations.Inventory.StockTakeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Operations.Inventory"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 20">
            <Image Source="/Views/CBC_Logo_500x500.png"
                   Width="50"
                   Height="50"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality"/>
            <TextBlock Grid.Row="0"
                       Text="Stock Take"
                       Style="{StaticResource Heading2TextBlock}"
                       Margin="40 0 0 20"/>
            <TextBlock Text="{Binding ItemName}" 
                   FontWeight="Bold" 
                   FontSize="16"
                   Foreground="DarkSlateGray"
                   Grid.Row="0" />

        </StackPanel>


        <StackPanel Orientation="Horizontal" Margin="0,10,0,0" Grid.Row="1">
            <TextBlock Text="Recorded:" Width="100"/>
            <TextBox Text="{Binding RecordedQuantity}" IsReadOnly="True" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0,10,0,0" Grid.Row="2">
            <TextBlock Text="Counted:" Width="100"/>
            <TextBox Text="{Binding CountedQuantity}" Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"  Grid.Row="3">
            <Button Content="Save" Width="80" Height="30"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource PrimaryButton}"/>
            <Button Content="Cancel" Width="80" Height="30" Margin="5"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
