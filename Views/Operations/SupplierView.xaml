<UserControl x:Class="CbcRoastersErp.Views.Operations.SupplierView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views.Operations"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Suppliers" FontSize="24" FontWeight="Bold" Margin="0 0 0 10" />
            <DataGrid ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}" AutoGenerateColumns="False" Height="300">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Supplier_Name}" />
                    <DataGridTextColumn Header="Email" Binding="{Binding Contact_email}" />
                    <DataGridTextColumn Header="Phone" Binding="{Binding Contact_phone}" />
                    <DataGridTextColumn Header="Address" Binding="{Binding Address}" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" Margin="0 10 0 0" HorizontalAlignment="Left">
                <Button Content="Add" Command="{Binding AddCommand}" Margin="0 0 10 0" />
                <Button Content="Edit" Command="{Binding EditCommand}" Margin="0 0 10 0" />
                <Button Content="Delete" Command="{Binding DeleteCommand}" Margin="0 0 10 0" />
                <Button Content="Back" Command="{Binding BackCommand}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
