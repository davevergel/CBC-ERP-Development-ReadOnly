<UserControl x:Class="CbcRoastersErp.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CbcRoastersErp.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{DynamicResource BackgroundBrush}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20" VerticalAlignment="Top">
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 0 0 20">
                <Image Source="/Views/CBC_Logo_500x500.png"
                   Width="50"
                   Height="50"
                   Stretch="Uniform"
                   Margin="0 0 15 0"
                   RenderOptions.BitmapScalingMode="HighQuality" />
                <TextBlock Text="Artisan Executable Path" Style="{StaticResource Heading1TextBlock}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBox Text="{Binding ArtisanPath, UpdateSourceTrigger=PropertyChanged}" Width="400" Margin="0,5,0,5" />
                <Button Content="Browse" Style="{StaticResource SecondaryButton}" Command="{Binding BrowseCommand}" Margin="0,0,10,0" Width="100"/>
            </StackPanel>

            <TextBlock Text="Operator" FontWeight="Bold" Margin="0,10,0,0" />
            <TextBox Text="{Binding Operator}" Width="400" Margin="0,5,0,5" />

            <TextBlock Text="Default Origin" FontWeight="Bold" />
            <TextBox Text="{Binding DefaultOrigin}" Width="400" Margin="0,5,0,5" />

            <TextBlock Text="Default Roast Weight (g)" FontWeight="Bold" />
            <TextBox Text="{Binding DefaultWeight}" Width="400" Margin="0,5,0,5" />

            <TextBlock Text="Roast Template Path (.alog/.json)" FontWeight="Bold" Margin="0,10,0,0" />
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBox Text="{Binding TemplatePath}" Width="400" Margin="0,5,0,5" />
                <Button Style="{StaticResource SecondaryButton}" Content="Browse Template" Command="{Binding BrowseTemplateCommand}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                
                <Button Content="Save" Style="{StaticResource SecondaryButton}" Command="{Binding SaveCommand}" Margin="0,0,10,0" Width="100"/>
                <Button Content="Back" Style="{StaticResource SecondaryButton}" Command="{Binding NavigateBackCommand}" Margin="0,0,10,0" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
